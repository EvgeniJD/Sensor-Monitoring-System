let sensorIntervalID = 0;

let sensorId;
const sensorName = 'sensor_3';

let temp = 30;
let hum = 70;

const variables = {
    sensorIntervalID,
    sensorId,
    sensorName,
    temp,
    hum
};

export default variables;